// 使用css-loader处理css文件
// import 'css-loader!../css/login.css';
import '../css/login.css';

const sum = (m, n) => {
    return m + n;
};

const square = (m) => {
    return m * m;
};

export { sum, square };
